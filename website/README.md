# Project page

Live (GitHub Pages): **https://anonymouspubcode.github.io/ICLR27-AlienBody/**

Source of truth for the page is the repo-root [`index.html`](../index.html) (so `assets/` resolves under Pages).

This folder keeps `style.css`; `index.html` here only redirects to the root page.
