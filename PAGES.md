# GitHub Pages

**Live:** https://anonymouspubcode.github.io/ICLR27-AlienBody/

Configured as a project site from branch `main`, folder `/` (repo root).
The landing page is root [`index.html`](index.html); media lives under [`assets/`](assets/).

Re-enable / inspect via API or UI: **Settings → Pages → Deploy from branch → `main` / `/ (root)`**.
